import React from 'react';
import { Typography } from '@mui/material';

const WatchedMovies = () => {
   return (
      <Typography style={{ fontWeight: '600', marginLeft: '40px' }}>
         My Watched Films
      </Typography>
   );
};

export default WatchedMovies;
